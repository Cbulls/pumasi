# 백엔드(pumasi-egov) 주소. 복사해서 .env.local 로 사용.
NEXT_PUBLIC_API_BASE=http://localhost:8080

# 공개 사이트 URL (canonical / OG / sitemap). 배포 시 실제 도메인으로 교체.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# 데모 계정 스위처·자동 로그인. 프로덕션은 false (이메일 매직링크만).
NEXT_PUBLIC_DEMO_AUTH=true
