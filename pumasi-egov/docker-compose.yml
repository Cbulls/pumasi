services:
  db:
    image: postgres:16
    container_name: pumasi-db
    environment:
      POSTGRES_DB: pumasi
      POSTGRES_USER: pumasi
      POSTGRES_PASSWORD: pumasi
    ports:
      - "5432:5432"
    volumes:
      - pumasi-pgdata:/var/lib/postgresql/data

volumes:
  pumasi-pgdata:
